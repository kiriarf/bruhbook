<p id="notice"><%= notice %></p>

<h1>Posts</h1>

    <% @posts.each do |post| %>
        <p>Text content <%= post.text_content %></p>
        <p>Pics <%= post.img_url %></p>
        <p>Bullshit bruhs <%= post.bullshit_bruhs %></p>
        <p>user <%= post.user_id %></p>
        <p><%= post.likes.count %> <%= (post.likes.count) == 1 ? 'Yes Bruh' : 'Yes Bruhs'%></p>
        <p><%= post.bullshits.count %> <%= (post.bullshits.count) == 1 ? 'Bullshit' : 'Bullshits'%></p>
        <p>
        <%= link_to 'Show', post %>
        <%= link_to 'Edit', edit_post_path(post) %>
        <%= link_to 'Delete', post, method: :delete, data: { confirm: 'Are you sure?' } %>
        </p>
      </tr>
    <% end %>

<%# 
    <%= image_tag("makersNews.jpeg") %> 
    <%# <%= image_tag("bruh_pic.gif") %>


<br>


<%= link_to 'New Post', new_post_path %>
